# Installed as /etc/nginx/conf.d/plan-review-upload-limits.conf.
#
# This is the total request size, not a per-file size. It supports a useful
# batch of code books and plan documents while still placing a finite bound
# on request buffering and application memory use.
client_max_body_size 100m;
